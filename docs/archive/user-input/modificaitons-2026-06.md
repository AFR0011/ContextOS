1) Daily timeline should be just tasks with optional times (no start/finish, just one time), and it should be more similar to writing to-do lists in the notepad (so I can create/remove tasks just as easily and seamlessly, and checking them crosses/uncrosses them instead of completely removing them from the UI).
2) Revamp deadlines/dates; "Dates" should only include dates that are important (e.g., final exam date, flight date), not necessarily deadlines. But, now tasks can have due dates and we can have another toggleable section called "tasks" which will include all tasks, not just today's. 
3) Move active tasks and deadlines to the top in project pages, remove notes/decisions (freeform recovery notes do the same job but better).
4) Remove "top priorities" completely.
5) Add the quick capture field above the dashboard.
6) Calendar timeline view, only include the times where something is scheduled, no need to display the full day from 6 to 22.