# Stage 8.3 CI Recheck

This marker records that the Stage 8.3 v4 candidate requires a fresh full CI run after aligning the existing shell-version assertions. It does not change application behavior.
