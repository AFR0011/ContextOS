# Stage 8 hosted preview tests

This directory contains the Stage 8.2 hosted-preview smoke coverage. Tests are executed only against an explicitly designated non-production Vercel preview backed by a dedicated preview database. They must never target a production alias or production database.
